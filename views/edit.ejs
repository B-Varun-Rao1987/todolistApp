<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TO DO LIST</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            height: 100vh;
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: Arial, Helvetica, sans-serif;
        }

        .container {
            height: 200px;
            width: 60%;
            background-color: #74531e;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .container h2 {
            color: #dfd7d7;
            margin-bottom: 25px;
            letter-spacing: 2px;
        }

        .container .editTask {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
        }

        .editTask .editText {
            width: 60%;
            padding: 8px;
            font-size: 20px;
            border: none;
        }

        .editBtn,
        .backBtn {
            padding: 8px;
            font-size: 18px;
            height: 40px;
            border: none;
            width: 7%;
            font-weight: 700;
            transition: background-color 500ms;
        }

        .editBtn {
            cursor: pointer;
            background-color: #dea23c;
        }

        .editBtn:hover {
            background-color: #c89337;
        }

        .backBtn {
            text-decoration: none;
            text-align: center;
            color: black;
            background-color: #c97e4d;
        }

        .backBtn:hover {
            background-color: #9e5626;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2>EDIT TASK</h2>
        <form action="/todolist/<%= todolist._id %>?_method=PUT" class="editTask" method="POST">
            <input type="text" class="editText" value="<%= todolist.title %>" name="todolist[title]">
            <button class="editBtn">Edit</button>
            <a href="/todolist" class="backBtn">Back</a>
        </form>
    </div>
    <%- include('../partials/footer') %>